# Development Store Assets

This directory is reserved for temporary/local artwork used during development.

Do not use files from this directory as production Google Play listing assets.

Expected structure:

- `icon/` — development icon variants
- `feature-graphic/` — development feature graphic variants
- `screenshots/` — development screenshots

Production-ready artwork belongs under `public/store-assets/production/`.
